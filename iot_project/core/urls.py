from django.urls import path
from . import views

# This app is part of the project's root URLs, so no specific 'app_name' is usually needed here
# unless you want to namespace specific core views. For now, directly include in project urls.
urlpatterns = [
    # Already included in iot_project/urls.py
]